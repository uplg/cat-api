services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cat-monitor-backend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      - ./devices.json:/app/devices.json
      - ./device-cache.json:/app/device-cache.json
    networks:
      - cat-monitor-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: cat-monitor-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - cat-monitor-network

networks:
  cat-monitor-network:
    driver: bridge
